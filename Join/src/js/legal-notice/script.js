/**
 * The function toggles the visibility of the elements with the IDs 'impressum_includer' and
 * 'privacy_policy_includer'.
 */
function togglePrivacyPolicy() {
    document.getElementById('impressum_includer').classList.toggle('d-none');
    document.getElementById('privacy_policy_includer').classList.toggle('d-none');
}