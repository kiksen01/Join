class Keyboard{

    //Keyboard Key Class
    /* In the given code, `LEFT = false; RIGHT = false; SPACE = false; A = false; D = false; F = false;
    P = false;` is initializing the properties of the `Keyboard` class with the value `false`. These
    properties represent different keys on a keyboard and are used to keep track of whether a
    particular key is currently pressed or not. By setting them to `false` initially, it indicates
    that none of the keys are pressed at the start. */
    LEFT = false;
    RIGHT = false;
    SPACE = false;
    A = false;
    D = false;
    F = false;
    P = false;
}